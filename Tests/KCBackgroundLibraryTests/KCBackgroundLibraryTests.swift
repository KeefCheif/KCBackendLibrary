import XCTest
@testable import KCBackgroundLibrary

final class KCBackgroundLibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(KCBackgroundLibrary().text, "Hello, World!")
    }
}
